# Nginx snippet for filemanager_project
# Generated by bootstrap_phase3_overrides.sh
#
# Variables are sourced from: /home/ubuntu/my_services/.uspp/domains/projects.domains.env
#
# Expected:
#   PUBLIC_PORT_filemanager_project=<host_port>
#   Either:
#     CUSTOM_DOMAIN_filemanager_project=example.com
#   or:
#     SUBDOMAIN_filemanager_project=filemanager_project  (will become SUBDOMAIN.BASE_DOMAIN)
#
# This file is a template. Use the generator: /home/ubuntu/my_services/project_wizard.sh nginx

server {
    listen 80;
    server_name __SERVER_NAME__;

    client_max_body_size 50m;

    location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_pass http://127.0.0.1:__PUBLIC_PORT__;
    }
}
